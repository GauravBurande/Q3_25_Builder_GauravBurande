[toolchain]
package_manager = "pnpm"

[features]
resolution = true
skip-lint = false

[programs.localnet]
spliff = "HFdT7mY2w8TpFJK23B26wQrCkHD7BdVdG3mrCzu9uAzA"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "localnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "pnpm exec ts-mocha -p ./tsconfig.json -t 1000000 tests/spliff.ts"
surftest = "pnpm exec ts-mocha -p ./tsconfig.json -t 1000000 tests/surfnet.test.ts"
